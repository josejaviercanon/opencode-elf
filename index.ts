// Entry point for OpenCode global plugin discovery.
// OpenCode loads this file when the plugin directory is discovered; the built
// plugin lives in dist/.
export { default } from "./dist/index.js";
